import Button from '@mui/material/Button';
import { styled } from '@mui/system';

const QuantityButtons = styled('span')( ({theme}) => ({    
    borderRadius: "50%",
    width: "19px",
    height: "19px",
    fontSize: "13px",
    background: "#000",
    color: "#fff",
    textAlign:"center"
    
}))

export default QuantityButtons